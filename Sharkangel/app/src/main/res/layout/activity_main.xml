<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical"
    android:background="@drawable/fish2">

    <TextView
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="iSharkInfo"
        android:textSize="50dp"
        android:layout_alignParentTop="true"
        android:layout_marginTop="43dp"
        android:textColor="#e9da0fe8"
        android:id="@+id/textView"
        android:layout_alignParentEnd="true"
        android:layout_alignParentStart="true"
        android:gravity="center"
        android:textStyle="bold|italic" />

    <Button android:id="@+id/btnUploadData"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Take pictures and upload"
        android:width="250dp"
        android:height="50dp"
        android:layout_above="@+id/btnViewData"
        android:layout_alignStart="@+id/btnViewData"
        android:textColor="#333333" />

    <Button android:id="@+id/btnViewData"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="View Profile"
        android:width="250dp"
        android:height="50dp"
        android:layout_centerVertical="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="60dp"
        android:layout_marginBottom="60dp"
        android:textColor="#333333" />

    <Button android:id="@+id/btnProhibitFish"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Ban shark species"
        android:width="250dp"
        android:height="50dp"
        android:layout_below="@+id/btnViewData"
        android:layout_alignStart="@+id/btnViewData"
        android:textColor="#333333" />

</RelativeLayout>